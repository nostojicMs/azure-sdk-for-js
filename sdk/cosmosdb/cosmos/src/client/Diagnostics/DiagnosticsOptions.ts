// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.

import { SharedOptions } from "../../request/SharedOptions";


/**
 * Options that can be specified for a requested issued to the Azure Cosmos DB servers.=
 */
export interface DiagnosticsOptions extends SharedOptions {
 }
export interface CosmosDiagnostics extends DiagnosticsOptions {
 }
